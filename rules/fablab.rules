
/*rule "Change_Contact_State"
        when 
                Item Door_Main received command
        then 
		logDebug("Door","command"+command)
                if (command == 1){
                        sendCommand(Door_Main,CLOSED)
                }else{
                        sendCommand(Door_Main,OPEN)
                }

end*/
